export default function LessonCardBtn() {
  return (
    <div
      style={{
        padding: ".8rem 2rem",
        border: "1px solid #703EDB",
        borderRadius: "5px",
        color: "#703EDB",
        fontSize: "1rem",
        textAlign: "Center",
        fontWeight: "500",
        cursor: "pointer",
        onHover: {
          backgroundColor: "black",
        },
      }}>
      Class Details
    </div>
  );
}
